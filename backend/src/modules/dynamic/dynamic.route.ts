// import { Router } from "express";
// import { DynamicController } from "./dynamic.controller";

// const router = Router();

// router.post("/:type", DynamicController.createListData);
// router.get("/:type", DynamicController.getListData);

// export default router;
